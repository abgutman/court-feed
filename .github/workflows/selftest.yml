name: Retry self-test

# TEMPORARY — deliberately fails so we can watch retry.yml re-run it.
# Delete this file (and its entry in retry.yml) after the test.

on:
  workflow_dispatch:

jobs:
  fail:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - run: |
          echo "Deliberate failure to exercise the retry workflow (attempt $GITHUB_RUN_ATTEMPT)"
          exit 1
